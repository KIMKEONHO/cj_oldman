<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- 로그 파일 경로 (없으면 현재 디렉터리 기준 logs/) -->
    <springProperty name="LOG_PATH" source="logging.file.path" defaultValue="logs"/>
    <property name="LOG_FILE" value="${LOG_PATH}/cj_alimtalk.log"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%c] - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 파일 출력 (일별 롤링, 30일 보관) -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/cj_alimtalk.%d{yyyy-MM-dd}.log</fileNamePattern>
<!--            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">-->
<!--                <maxFileSize>10MB</maxFileSize>-->
<!--            </timeBasedFileNamingAndTriggeringPolicy>-->
<!--            <maxHistory>30</maxHistory>-->
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%c] - %msg%n</pattern>
<!--            <charset>UTF-8</charset>-->
        </encoder>
    </appender>

    <!-- 프로젝트 패키지 레벨 -->
    <logger name="com.example.cj_alimtalk_service" level="INFO"/>

    <logger name="jdbc.sqlonly" level="INFO"/>

    <logger name="org.bgee.log4jdbc" level="DEBUG"/>

    <logger name="jdbc.resultsettable" level="INFO"/>

    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>
